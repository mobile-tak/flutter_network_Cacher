name: example
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.17.3 <3.0.0"


dependencies:
  extended_image: ^6.2.1
  cached_network_image: ^3.2.1
  http: ^0.13.4
  dio: ^4.0.6
  # flutter_network_cacher:
  #   path: ../../../flutter_network_cacher
  objectbox: ^1.6.0
  objectbox_flutter_libs: any
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

dev_dependencies:
  build_runner: ^2.0.0
  objectbox_generator: any
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true


objectbox:
  # Writes objectbox-model.json and objectbox.g.dart to lib/custom (and test/custom).
  output_dir: objbox/model/generated